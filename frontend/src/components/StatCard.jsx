export default function StatCard({label,value,icon:Icon}){return <div className="stat-card"><div><span className="muted">{label}</span><strong>{value}</strong></div>{Icon&&<div className="stat-icon"><Icon size={20}/></div>}</div>}
